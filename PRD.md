# LLM-Guardian 大模型效能监测平台 · 产品需求文档 (PRD)

---

## 1. 产品概述
* **产品名称**：LLM-Guardian 大模型效能监测平台 (LLM-Guardian Enterprise)
* **发布版本**：v1.2.0 (Enterprise)
* **时区定位**：统一采用北京时间 (UTC+8 / Asia/Shanghai) 进行全局指标存储、日志过滤及报表展示

---

## 2. 系统技术栈
LLM-Guardian 采用现代而高效的轻量级前后端合一与持久化存储技术栈，保障高效响应与平滑的数据分析。

* **前端框架**：`React 18` + `Vite` + `TypeScript` 开发的高性能单页架构 (SPA)
* **交互与动画**：`motion` (来自 `motion/react`) 提供硬件加速的微交互与视窗转场过度
* **组件与图表**：`Recharts` + `D3.js` 提供高细腻度时序波形图、合规曲线与瀑布负载分析图表
* **UI 与样式**：`Tailwind CSS` 最新样式引擎与 `Lucide React` 精确图标底座
* **本地与云端存储**：前端集成内置自愈系统的 `localStorage` 缓存机制，支持持久、稳定、多渠道、长周期探测日志和告警订阅机制的完整存取

---

## 3. 系统各模块功能说明

项目共分为六个核心业务功能模块，统一呈现在现代精细的仪表盘左侧控制边栏：

```
LLM-Guardian Enterprise 
 ├── 监控面板
 │    ├── 1. 实时大盘 (Dashboard)
 │    ├── 2. 渠道管理 (Channels)
 │    └── 3. 拨测配置 (Config Dialer)
 └── 分析与告警
      ├── 4. 时序测试日志 (Telemetry Logs)
      ├── 5. 联动告警终端 (Alert Hub)
      └── 6. SLA 合规审计 (Compliance Audits)
```

---

### 3.1 模块 1：实时大盘 (Dashboard)
* **功能描述**：大模型服务的“首屏仪表盘”，负责提供全平台集群当前健康状态、QPS吞吐曲线及流式响应性能指标。
* **业务细节**：
  * **快速性能指标**：展示当前系统总探测次数、合规拦截比例等。
  * **24h时序波形图**：展示多实例 E2E 延迟、首字时延（TTFT）及吞吐时序图波形。
  * **集群状态底座**：右上角动态呼吸灯实时呈现当前节点心跳状况，标识时区为 `北京时间 (UTC+8)`。

---

### 3.2 模块 2：渠道管理 (Model Channels)
* **功能描述**：管理并维护大模型渠道。
* **业务细节**：
  * **实例配置卡片**：支持新增、编辑、删除推理宿主机或 API 接口别名（如 `DeepSeek-V3 (Private Edge)`、`Gemini-Pro-External` 等）。
  * **健康度感知**：根据最近 1 小时主动拨测的达标状态实时标定 `ACTIVE` (激活运行中) 或 `FAIL/WARNING` (过载/中断)。
  * **配置细节**：支持自定义模型类别、请求并发配额限速阈值等。

---

### 3.3 模块 3：拨测配置 (Config Dialer Task)
* **功能描述**：设计并执行时序主动探测任务，支持模拟多并发。
* **业务细节**：
  * **新建拨测表单**：允许 DevOps 设置拨测频率（秒/分钟级）、针对特定推理渠道自动配对测试数据集 (Prompt Dataset)、以及在反向代理层强制开启或关闭 HMR/并发压力策略。
  * **性能期望设置**：设置 SLA 安全底线（首字延迟安全阈值、总耗时最长时限、最低 TPS 水平），为模型判定指标背离（SLA Violation）提供合规基准线。

---

### 3.4 模块 4：时序测试日志 (Telemetry Logs)
* **功能描述**：流式回溯每一次主动拨测的时序结果，定位性能异常。
* **业务细节**：
  * **北京时间流式列表**：日志的发起时间采用 `Asia/Shanghai` 时区，支持分秒级高保真还原。
  * **多维多级过滤**：
    1. **模型渠道筛选**：按特定实例筛查性能数据。
    2. **指标状态过滤**：一键过滤 `全部`、`成功`、`失败`、以及核心指标突破 SLA 安全阈值的 `背离违反 (Violation)` 日志。
    3. **时间范围过滤**：快速选定 `近1小时`、`近6h`、`近24h`、`近7天` 极速收敛问题区间。
  * **瀑布时序抽屉**：点击任意探测日志，浮现抽屉卡片，透视该次请求的 TTFT 耗时、多线程交互记录等。

---

### 3.5 模块 5：联动告警终端 (Alert Hub)
* **功能描述**：多渠道推送模型集群的崩溃和违约通报。
* **业务细节**：
  * **告警日志馈送**：提供最近捕获的违约行为，精确展示出错时间（北京时间）、接收群名称（如 `DevOps 微信工作群`、`AI 基础运维钉钉群`）。
  * **订阅群组管理**：支持一键修改、绑定、开启或静默飞书、钉钉、企业微信等即时通讯工具的推送 Bot。

---

### 3.6 模块 6：SLA 合规审计 (Compliance Audits)
* **功能描述**：面向运维决策层、提供聚合周期的资源综合表现审计以及针对性能瓶颈的最佳算力调优决策卡片。
* **核心卡片布局**：
  1. **综合 SLA 达标指数**：长周期内达标率的复合权重数值（如 `97.27%%`），精准感知可用高度。
  2. **平台纳管模型 SLA 指标综合均值**：
     * **首字延时 (TTFT) 均值**：监控大模型首字吞吐是否顺畅。
     * **吞吐速率 (TPS) 均值**：评估推理显卡的计算效率（以 Tok/s 为单位展示）。
     * **字间延迟 (ITL) 均值**：精确衡量生成的流畅度与断点摩擦。
     * **全网模型峰值延迟**：标定最恶劣情境下的时延。
     * 自动配合 **SLA 综合达标均值**（如 `97.27%`），反馈平台集群环境处于健康水位还是需要增购或按需保活（Warm-up）。
  3. **模型集群整体多维评估与调优建议**：根据上述指标，自动通过模型实例故障分布生成契合业务的、无需外部云引擎参与的主动决策策略（如冷温保活、副本空载弹性收缩，并发限流配置等）。

---

## 4. 非功能性需求
1. **统一时间制式**：在日志显示、波形时间轴、报警日志、PDF 报表全链路对齐北京时间，降低两地时区转换带来的运维调试和汇报成本。
2. **零阻碍企业报表**：PDF 导出组件完全在本地计算，规避了由于中文字体加载失败或编码缺失导致的乱码缺陷，生成完全矢量高保真的 PDF 文档。
3. **响应式宽屏与敏捷触控**：界面对 1080P/2K 桌面控制大屏提供极佳的伸缩排布，同时对平板和手机提供不低于 44px 的大按钮方便点触。
